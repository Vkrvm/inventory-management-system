# ============================================
# SUPABASE DATABASE CONFIGURATION
# ============================================
# Get these from your Supabase project settings:
# Project Settings -> Database -> Connection string

# Connection Pooler URL (for Prisma Client - use Transaction mode)
# This is used for application queries (serverless-friendly)
DATABASE_URL="postgresql://postgres.your-project-ref:[YOUR-PASSWORD]@aws-0-us-east-1.pooler.supabase.com:6543/postgres?pgbouncer=true&connection_limit=1"

# Direct Connection URL (for Prisma Migrations - use Session mode)
# This is used for running migrations
DIRECT_URL="postgresql://postgres.your-project-ref:[YOUR-PASSWORD]@aws-0-us-east-1.pooler.supabase.com:5432/postgres"

# Alternative format (if not using connection pooler):
# DATABASE_URL="postgres://postgres:[YOUR-PASSWORD]@db.your-project-ref.supabase.co:5432/postgres"
# DIRECT_URL="postgres://postgres:[YOUR-PASSWORD]@db.your-project-ref.supabase.co:5432/postgres"

# How to get your Supabase credentials:
# 1. Go to https://app.supabase.com/
# 2. Select your project
# 3. Go to Settings -> Database
# 4. Copy "Connection string" and replace [YOUR-PASSWORD] with your database password

# ============================================
# NEXTAUTH CONFIGURATION
# ============================================
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-secret-key-here-generate-with-openssl-rand-base64-32"

# Generate NEXTAUTH_SECRET with one of these methods:
# 1. OpenSSL: openssl rand -base64 32
# 2. Online: https://generate-secret.vercel.app/32
# 3. Node: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
